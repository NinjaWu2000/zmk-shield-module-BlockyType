if SHIELD_BLOCKYTYPE_RIGHT

    # Name must be less than 16 characters long!
    config ZMK_KEYBOARD_NAME
        default "BlockyType"

    config ZMK_SPLIT_ROLE_CENTRAL
        default y

endif

if SHIELD_BLOCKYTYPE_LEFT
    config ZMK_SPLIT_ROLE_CENTRAL
        default n


endif

if SHIELD_BLOCKYTYPE_LEFT || SHIELD_BLOCKYTYPE_RIGHT

    config ZMK_SPLIT
        default n

endif
